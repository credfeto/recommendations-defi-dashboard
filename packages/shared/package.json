{
  "name": "@defi-dashboard/shared",
  "version": "0.1.0",
  "description": "Shared types and utilities for DeFi Dashboard",
  "types": "src/index.ts",
  "main": "src/index.ts",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.build.json"
  }
}
